---
title: "Sharp Damage in Zenless Zone Zero — Laceration and Maim DMG Explained"
slug: "sharp-damage-zzz"
description: "A complete analysis of Sharp Damage in Zenless Zone Zero, the new Armorer mechanic, and how it interacts with Laceration DMG and Maim DMG in-game, including the formulas behind the damage calculation."
cover: "/resources/images/guides/sharp-damage-zzz.png"
tags: ["Armorer", "Sharp DMG", "Laceration DMG", "Claret Flint"]
date: "2026-09-24"
published: true
toc: true
---

**Sharp Damage** is a new damage type introduced alongside the **Armorer** specialty in Zenless Zone Zero Version 3.2, with Claret becoming the first agent of this new specialty. Unlike standard damage, **Sharp DMG** scales with DEF instead of ATK and replaces the traditional CRIT DMG system with Laceration DMG. To understand **how Sharp Damage works**, it is first important to look at what makes **Armorers** different from standard agents and how their damage system is structured.

## Introduction to Armorers

Every agent in ZZZ deals damage through a standard formula:

```text
Standard DMG = (ATK × SkillMult) × DefMult × DMG% × CritMult × ResMult × StunMult × Direct DMG%
```

Where `ATK` represents the agent's Attack, while `CritMult` is the critical hit multiplier determined by the hit's CRIT result and the agent's CRIT DMG. However, Armorer agents change a few of these fundamental pieces to build their damage formula:

| Standard Agent    | Armorer Agent           |
| ----------------- | ----------------------- |
| Scales with ATK   | Scales with **DEF**     |
| Uses CRIT DMG     | Uses **Laceration DMG** |
| Single CRIT check | Double Laceration check |
| Normal DMG        | Sharp DMG               |

The result is a separate damage pipeline that can be represented as the **Sharp DMG Formula**. The interesting part is that, despite having its own mechanics, Sharp DMG still follows many of the same rules used by the game's existing damage systems. PEN, PEN Ratio, DEF Shred, RES Shred, and Stun Multiplier still play a role in this new mechanic. And, since I won my 50/50 for Clarett, I was able to spend some time in the Training Room testing these interactions firsthand.

## Sharp DMG Formula

After testing the different components individually, the complete **Sharp DMG formula** can be represented as:

```text
Sharp DMG = (DEF × SkillMult)
          × DefMult
          × DMG%
          × Lac₁
          × Lac₂
          × SharpDMG%
          × StunMult
          × ResMult
          × DirectDMG%
```

### Formula Breakdown

| Component       | Description                                                          |
| --------------- | -------------------------------------------------------------------- |
| DEF × SkillMult | Base skill multiplier damage. Uses Armorers' total DEF.              |
| DefMult         | Enemy defense multiplier.                                            |
| DMG%            | All damage bonuses (elemental, skill type, global, exclusive, etc.). |
| Lac₁            | First Laceration check (0%–100% CRIT Rate).                          |
| Lac₂            | Second Laceration check (100%–200% CRIT Rate).                       |
| SharpDMG%       | Damage bonus applied after Lac₂, analogous to Sheer DMG Bonus.       |
| StunMult        | Enemy stun multiplier.                                               |
| ResMult         | Enemy elemental resistance multiplier.                               |
| DirectDMG%      | Damage bonus coming from the Contamination state.                    |

At this point, the formula looks relatively simple. The real mystery starts with the two Laceration checks.

## How Sharp Damage works in ZZZ

This is where **Sharp DMG** gets a little more interesting. The game actually gives us a pretty good hint about what's happening here in Claret's passive:

> When a critical hit is triggered, Sharp DMG doesn’t receive the CRIT DMG Bonus and instead uses the Laceration DMG Bonus. If CRIT Rate exceeds 100% and a critical hit has already been triggered, an additional Laceration check is performed based on the excess CRIT Rate. If Laceration is triggered, the Laceration DMG Bonus is applied an additional time.

In other words, **Sharp DMG** doesn't simply have a single crit system; rather, the CRIT system itself is effectively being reused, with **Laceration DMG replacing CRIT DMG**, and excess CRIT Rate becoming relevant for a second check. Both Laceration checks work as follows:

### Lac₁ — First Laceration Check

The first check uses the standard CRIT Rate range from 0% to 100%:

```text
Lac₁ = 1 + min(max(CRIT Rate, 0), 1) × Laceration DMG
```

- At 0% CRIT Rate: `Lac₁ = 1` (no bonus).
- At 50% CRIT Rate: `Lac₁ = 1 + 0.5 × LacDMG` (probabilistic).
- At 100% CRIT Rate or higher: `Lac₁ = 1 + 1.0 × LacDMG` (guaranteed, capped).

### Lac₂ — Second Laceration Check

Once CRIT Rate goes beyond 100%, the excess CRIT Rate becomes the basis for the second Laceration check:

```text
Lac₂ = 1 + min(max(CRIT Rate − 1, 0), 1) × Laceration DMG
```

- At 100% CRIT Rate: `Lac₂ = 1` (no bonus).
- At 150% CRIT Rate: `Lac₂ = 1 + 0.5 × LacDMG` (probabilistic).
- At 200% CRIT Rate or higher: `Lac₂ = 1 + 1.0 × LacDMG` (guaranteed, capped).

So, in simple terms:

#### 0–100% CRIT Rate → first check

If only Lac₁ succeeds, the damage number is followed by **`!!`** in-game.

#### 100–200% CRIT Rate → second check

If both Lac₁ and Lac₂ succeed, the damage number is followed by **`!!!`** in-game.

#### 200%+ CRIT Rate → no additional benefit

One of the things I had to clarify was a possible third Laceration check, but I had no way to do it without Claret's Signature W-Engine. However, with some help from Ben, I was able to determine that **CRIT Rate above 200% does not provide another Laceration check** nor increase the second one beyond 100%.

So, for **Sharp Damage**, 200% CRIT Rate is effectively the ceiling for the Laceration system. Beyond 200%, additional CRIT Rate provides no further benefit to Sharp DMG or its Laceration checks, making any CRIT Rate above this threshold effectively wasted for an Armorer's build.

## Laceration DMG

**Laceration DMG** is the stat that replaces CRIT DMG for Armorers. Unlike CRIT DMG, which is a fixed percentage tied to a single CRIT check, Laceration DMG determines the multiplier applied when a Laceration check succeeds. It is applied once when Lac₁ succeeds, and applied again as a separate multiplication when Lac₂ succeeds.

<div style={{ height: "var(--padding-6px)" }} />

Claret's base Laceration DMG is 150%.. So, at 150% Laceration DMG, each successful check is:

```text
1 + 1.5 = 2.5×
```

If both Laceration checks succeed to trigger Sharp DMG:

```text
2.5 × 2.5 = 6.25×
```

<div style={{ height: "var(--padding-6px)" }} />

Laceration DMG can be increased through a few sources in-game, like Claret's own Additional Ability: Bloodline Legacy, which grants 25% Laceration DMG when another squad member is a Stun or Armorer character. Multiple Laceration DMG sources add together.

For example, the laceration check with Additional Ability: Bloodline Legacy active would be:

```text
150% + 25% = 175%
```

At 175% Laceration DMG, each successful check becomes:

```text
1 + 1.75 = 2.75×
```

Meanwhile, if both checks succeed:

```text
2.75 × 2.75 = 7.5625×
```

<div style={{ height: "var(--padding-6px)" }} />

So, the higher the **Laceration DMG** stat, the greater its impact on final damage, since each successful Laceration check applies the full Laceration DMG multiplier. This makes Laceration DMG particularly valuable for Armorers, as the stat can be applied independently by both Laceration checks when both are triggered.

## Maim DMG

**Maim** is a special damage instance triggered by specific Armorer skills when the enemy has **Gash** stacks, with a maximum of 3 stacks. When I started looking into Maim, I wasn't really sure whether it would follow its own damage formula or behave more like the other specialized damage types in ZZZ. Turns out, it was much simpler than I expected.

Unlike Remielle's Luminize, the **Maim DMG formula** is practically the same as the normal **Sharp DMG formula**, with the only real change being the multiplier used in calculations. The complete **Maim DMG formula** can be represented as:

```text
Maim DMG = (DEF × MaimMult)
         × DefMult
         × DMG%
         × Lac₁
         × Lac₂
         × SharpDMG%
         × StunMult
         × ResMult
         × DirectDMG%
```

**Maim** uses the same Laceration system as normal **Sharp DMG**, and Laceration is still rolled for each Maim instance, which means it also benefits from the CRIT Rate the agent has. Maim is treated as a separate damage instance, using its own skill-specific **Maim Multiplier**, which is defined by the level of the skill that contains it.

### Formula Breakdown

| Component      | Description                                                          |
| -------------- | -------------------------------------------------------------------- |
| DEF × MaimMult | Base skill multiplier Maim damage. Uses Armorers' total DEF.         |
| DefMult        | Enemy defense multiplier.                                            |
| DMG%           | All damage bonuses (elemental, skill type, global, exclusive, etc.). |
| Lac₁           | First Laceration check (0%–100% CRIT Rate).                          |
| Lac₂           | Second Laceration check (100%–200% CRIT Rate).                       |
| SharpDMG%      | Damage bonus applied after Lac₂, analogous to Sheer DMG Bonus.       |
| StunMult       | Enemy stun multiplier.                                               |
| ResMult        | Enemy elemental resistance multiplier.                               |
| DirectDMG%     | Damage bonus coming from the Contamination state.                    |

For example, _Special Attack: Bloodbloom Oath – Cleaving Gold and Iron_ has a Maim multiplier ranging from 812.7% at Level 1 to 1921.2% at Level 16. Each consumed **Gash** stack triggers one **Maim** damage instance using the **Maim Multiplier** of the skill that triggered it. Since Claret can consume up to 3 Gash stacks at once, a single skill can generate up to 3 Maim instances.

## Sharp DMG Bonus

**Sharp DMG Bonus** is an in-game bonus exclusive to Armorer agents, and is effectively the equivalent of what Sheer DMG Bonus is for Rupture Agents. Unlike regular DMG Bonus values, **Sharp DMG Bonus** is not added to the general DMG% pool. Instead, it acts as a separate multiplicative modifier after both Laceration checks.

```text
SharpDMG = 1 + SharpDMG%
```

This means its value is much higher than that of an Electric DMG Bonus, which would be applied much earlier in the **Sharp DMG formula**. However, currently the only source of **Sharp DMG Bonus** is Claret's Signature W-Engine, Crimson Thirst, which grants a percentage of Electric Sharp DMG after using a Special Attack or triggering Maim

## Final Sharp DMG Formula

So, to wrap up, the complete **Sharp DMG formula** is as follows:

```text
Sharp DMG = (DEF × SkillMult)
          × [ALC / (ALC + max(0, (EnemyDEF × (1 − DefShred) × (1 − PEN Ratio)) − PEN))]
          × (1 + DMG%)
          × (1 + min(max(CR, 0), 1) × LacDMG)
          × (1 + min(max(CR − 1, 0), 1) × LacDMG)
          × (1 + SharpDMG%)
          × (1 + Stun / 1)
          × (1 − (EnemyRES − RESShred))
```

For **Maim DMG**, `SkillMult` should be replaced by `MaimMult`:

```text
Maim DMG = (DEF × MaimMult)
         × DefMult
         × DMG%
         × (1 + min(max(CR, 0), 1) × LacDMG)
         × (1 + min(max(CR − 1, 0), 1) × LacDMG)
         × (1 + SharpDMG%)
         × (1 + Stun / 100)
         × (1 − (EnemyRES − RESShred))
```

## Final Notes

This article is based on experimental testing, in-game observations, official in-game descriptions, and reverse-engineering of Claret's damage pipeline in Zenless Zone Zero. The tested values consistently matched the proposed formulas, with only minor differences caused by in-game rounding.

These formulas are also implemented in the Z-TUNNER Damage Calculator, allowing you to test Sharp DMG and Maim interactions using your own Claret build. If you spot an incorrect number, interaction, or any other mistake, don't hesitate to let me know so I can fix it as soon as possible.
